[INCLUDES]
# No additional includes required

[Definition]
# Regular expression to detect failed login attempts in TigerVNC logs
failregex = ^.*Connection: AuthFailure from <HOST>$
ignoreregex =

# Optionally include any known harmless patterns
